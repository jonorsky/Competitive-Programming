#include <iostream>
#include <queue>
using namespace std;

int main(){
  struct Node{
    int key;
    struct Node *left, *right;
  };

  struct Node *newNode(int key){
    struct Node* temp = new Node;
    
  }

  return 0;
}
